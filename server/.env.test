# Variables de entorno para testing
NODE_ENV=test

# Base de datos de prueba (usar SQLite en memoria para pruebas rápidas)
TEST_DB_URL=sqlite::memory:
db_url=sqlite::memory:

# Puerto para servidor de pruebas
PORT=5001
TEST_API_URL=http://localhost:5001/api

# JWT Secret para pruebas
JWT_SECRET=test_jwt_secret_key_for_testing_only

# OpenRouter API (usar mock o clave de prueba)
offenrouter_api_key=test_api_key
llm_model=test/model
llm_base_url=http://localhost:3000/mock

# Email service (usar mock)
EMAIL_USER=test@example.com
EMAIL_PASS=test_password
EMAIL_HOST=smtp.test.com
EMAIL_PORT=587

# URLs de prueba
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:5001
